spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/airline_user_db
    username: root
    password : Agniswar@123
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format_sql: true
server:
  port: 5050
